<UserControl x:Class="ApprovalTools.Approve.Views.FolderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:ApprovalTools.Approve.ViewModels"
             mc:Ignorable="d" d:DataContext="{d:DesignInstance vm:FolderViewModel}"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <CheckBox x:Name="IsEnabled" VerticalAlignment="Center" Margin="0,2,4,0"/>
        <TextBlock Grid.Column="1" x:Name="Path" VerticalAlignment="Center"/>
    </Grid>
</UserControl>
